[build]
  base = "client"
  command = "npm ci && npm run build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  conditions = { NOT = [{"Cookie" = "nf_skip_redirect"}] }